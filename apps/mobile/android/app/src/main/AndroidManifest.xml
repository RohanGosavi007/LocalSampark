<manifest xmlns:android="http://schemas.android.com/apk/res/android" xmlns:tools="http://schemas.android.com/tools">
  <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
  <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
  <uses-permission android:name="android.permission.CAMERA"/>
  <uses-permission android:name="android.permission.INTERNET"/>
  <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS"/>
  <!-- Required from Android 13 (API 33). Without it every FCM notification is
       dropped silently, including the admin broadcast campaigns. -->
  <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
  <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32"/>
  <uses-permission android:name="android.permission.RECORD_AUDIO"/>
  <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>
  <uses-permission android:name="android.permission.VIBRATE"/>
  <!-- react-native-callkeep. Telecom refuses registerPhoneAccount() unless the
       app ships a ConnectionService guarded by BIND_TELECOM_CONNECTION_SERVICE
       (declared on the <service> below); without it setup() threw
       java.lang.SecurityException on the native modules thread, which is
       uncatchable from JS and killed the process at startup.
       FOREGROUND_SERVICE_MICROPHONE pairs with the only foregroundServiceType
       VoiceConnectionService actually uses (Constants.FOREGROUND_SERVICE_TYPE_MICROPHONE). -->
  <uses-permission android:name="android.permission.BIND_TELECOM_CONNECTION_SERVICE"/>
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE"/>
  <!-- Scoped storage replaced this from Android 10; declaring it unbounded
       triggers a Play Console policy warning. -->
  <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28"/>
  <queries>
    <intent>
      <action android:name="android.intent.action.VIEW"/>
      <category android:name="android.intent.category.BROWSABLE"/>
      <data android:scheme="https"/>
    </intent>
  </queries>
  <application android:name=".MainApplication" android:label="@string/app_name" android:icon="@mipmap/ic_launcher" android:roundIcon="@mipmap/ic_launcher_round" android:allowBackup="true" android:theme="@style/AppTheme" android:supportsRtl="true" android:enableOnBackInvokedCallback="false" android:fullBackupContent="@xml/secure_store_backup_rules" android:dataExtractionRules="@xml/secure_store_data_extraction_rules">
    <meta-data android:name="com.google.android.gms.wallet.api.enabled" android:value="true"/>
    <meta-data android:name="com.google.firebase.messaging.default_notification_icon" android:resource="@drawable/notification_icon"/>
    <meta-data android:name="expo.modules.notifications.default_notification_icon" android:resource="@drawable/notification_icon"/>
    <meta-data android:name="expo.modules.updates.ENABLED" android:value="false"/>
    <meta-data android:name="expo.modules.updates.EXPO_UPDATES_CHECK_ON_LAUNCH" android:value="ALWAYS"/>
    <meta-data android:name="expo.modules.updates.EXPO_UPDATES_LAUNCH_WAIT_MS" android:value="0"/>
    <activity android:name=".MainActivity" android:configChanges="keyboard|keyboardHidden|orientation|screenSize|screenLayout|uiMode|locale|layoutDirection" android:launchMode="singleTask" android:windowSoftInputMode="adjustResize" android:theme="@style/Theme.App.SplashScreen" android:exported="true" android:screenOrientation="portrait">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>
        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>
      <intent-filter>
        <action android:name="android.intent.action.VIEW"/>
        <category android:name="android.intent.category.DEFAULT"/>
        <category android:name="android.intent.category.BROWSABLE"/>
        <data android:scheme="localsampark"/>
      </intent-filter>
      <!--
        Android App Links for https://localsampark.in.

        Only the localsampark:// custom scheme was registered, so a shared web
        link always opened the browser rather than the app.

        autoVerify="true" makes Android fetch
        https://localsampark.in/.well-known/assetlinks.json at install time and
        check that it lists this package with the signing certificate SHA-256.
        That file is served from apps/web/public/.well-known/assetlinks.json.
        If verification fails the links simply fall back to the browser -- it
        cannot break the app -- but they will not open it either, so the
        fingerprint in that file has to match whatever key actually signs the
        shipped build (see android/KEYSTORE.md).
      -->
      <intent-filter android:autoVerify="true">
        <action android:name="android.intent.action.VIEW"/>
        <category android:name="android.intent.category.DEFAULT"/>
        <category android:name="android.intent.category.BROWSABLE"/>
        <data android:scheme="https" android:host="localsampark.in"/>
        <data android:scheme="https" android:host="www.localsampark.in"/>
      </intent-filter>
    </activity>
    <!-- react-native-callkeep ships this class but deliberately does not declare
         it: the ConnectionService must be registered by the app so Telecom can
         verify the BIND_TELECOM_CONNECTION_SERVICE guard. It was missing here
         entirely, so RNCallKeep.setup() -> registerPhoneAccount() threw
         SecurityException and crashed the app. android:exported must be true so
         the system Telecom stack can bind it; the android:permission guard is
         what keeps anything else out. -->
    <service
        android:name="io.wazo.callkeep.VoiceConnectionService"
        android:label="@string/app_name"
        android:permission="android.permission.BIND_TELECOM_CONNECTION_SERVICE"
        android:foregroundServiceType="microphone"
        android:exported="true">
      <intent-filter>
        <action android:name="android.telecom.ConnectionService"/>
      </intent-filter>
    </service>
  </application>
</manifest>